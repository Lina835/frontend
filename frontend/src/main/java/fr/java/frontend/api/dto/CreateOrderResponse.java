package fr.java.frontend.api.dto;
/**
 * DTO représentant la réponse renvoyée par le backend après la création d'une commande.
 */
public class CreateOrderResponse {
    // L'identifiant unique de la commande généré par la base de données (order_id)
    public int id;
}
